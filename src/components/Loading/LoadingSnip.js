import {VscLoading} from "react-icons/vsc";

const LoadingSnip = () => {
    return <span className={"mx-auto w-max"}>
        <VscLoading className={"animate-spin h-20 w-20 "} />
    </span>
}

export default LoadingSnip